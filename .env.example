# Copy to .env for local runs. Never commit the real file.
# The Supabase pooled connection string (Project settings -> Database -> Connection string).
DATABASE_URL=postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-ap-south-1.pooler.supabase.com:6543/postgres

# Set only when deliberately generating a test fixture. See scripts/simulate.py.
# APIX_ALLOW_SIMULATION=1
